services:
  - type: web
    name: ecommerce-app
    runtime: python3
    buildCommand: pip install -r requirements.txt && python render_init.py
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_DEBUG
        value: false
      - key: PYTHON_VERSION
        value: 3.14
    autoDeploy: true